=========================================
TODO
=========================================

1) Fix pip package manager
2) Fix llama.cpp version
3) Add psutil to venv. both for dev and prod. We need to get prod pip hash dependencies at this point.
4) Add pip-tools to dev. We need to get dev pip hash dependencies at this point.